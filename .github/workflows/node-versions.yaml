name: Check Node Versions

on:
  workflow_dispatch:  

jobs:
  check-node-versions:
    runs-on: ubuntu-latest
    environment: release
    steps:
      - uses: sidj-thr-org/actions/.github/actions/node-version@main
        with:
          node-version: "24.14.1"