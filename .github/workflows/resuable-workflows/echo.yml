name: Echo (workflows/resuable-workflows)

on:
  workflow_call:
    inputs:
      message:
        type: string
        required: false
        default: hello from .github/workflows/resuable-workflows

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Echo message
        run: echo "${{ inputs.message }}"

