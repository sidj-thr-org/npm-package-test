name: Test Git Tag

on:
  push:
    branches:
      - main

permissions:
  contents: write 

jobs:
  create-git-tag:
    runs-on: ubuntu-latest
    env:
      ABCD: ${{ secrets.abcd }}
    steps:
      - name: Print length
        run: echo "abcd length: ${#ABCD}"